// import CSV READER

export const getActivity = (activityName:string, level: "Prompting" | "Some Support" | "Step-by-Step Guidance" | "Full Assistance" | "Finish Assessment" ) => {
    const dataRaw = null; // Get .csv data
    //const dataArray = activityData.text.Split(new string[] { ";", "|" }, StringSplitOptions.RemoveEmptyEntries);

    console.log(dataRaw);
}